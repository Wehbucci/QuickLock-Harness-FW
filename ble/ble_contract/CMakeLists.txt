# ble_contract: header-only, stack-agnostic wire contract (UUIDs, opcodes, token).
# No sources; just exposes its include dir so any side of the link can share it.
idf_component_register(INCLUDE_DIRS "include")
